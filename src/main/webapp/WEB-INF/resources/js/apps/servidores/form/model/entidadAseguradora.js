define(['backbone'], function (Backbone) {

    var EntidadAseguradora = Backbone.Model.extend({
        className: 'edu.quipu.rrhh.models.EntidadAseguradora'
    });

    return EntidadAseguradora;
});
