define(['backbone'], function (Backbone) {

    var guardarDependencia = Backbone.Model.extend({

        className: 'edu.quipu.rrhh.models.EstadoCondicion'

    });

    return guardarDependencia;
});

