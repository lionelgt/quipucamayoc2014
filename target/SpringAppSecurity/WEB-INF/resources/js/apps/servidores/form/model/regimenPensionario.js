define(['backbone'], function (Backbone) {

    var RegimenPensionario = Backbone.Model.extend({
        className: 'edu.quipu.rrhh.models.RegimenPensionario'
    });

    return RegimenPensionario;
});