define(['backbone'], function (Backbone) {

    var UpdateHistorialUsuario = Backbone.Model.extend({

        className: 'edu.quipu.rrhh.models.HistorialPerfilUsuario'

    });

    return UpdateHistorialUsuario;
});
